select * from themepark;
--1
SELECT COUNT(PARK_CODE) FROM ATTRACTION;
--2
 SELECT COUNT(DISTINCT(PARK_CODE))
    FROM ATTRACTION;
--3
SELECT COUNT(PARK_CODE)
    FROM ATTRACTION;
--4
SELECT AVG(LINE_PRICE), SUM(LINE_QTY)
FROM SALES_LINE;

--5
SELECT PARK_CODE, MIN(TICKET_PRICE),MAX(TICKET_PRICE)
FROM TICKET
GROUP BY PARK_CODE;

--6
SELECT PARK_CODE, AVG(TICKET_PRICE)
FROM TICKET
GROUP BY PARK_CODE
HAVING AVG(TICKET_PRICE) >= 24.99;



--LAB TASKS
--QNO1:
SELECT AVG(HOUR_RATE) FROM HOURS;

--QNO2:
SELECT AVG(ATTRACT_AGE) FROM ATTRACTION WHERE PARK_CODE = 'UK3452';

--QNO3:
SELECT EMP_NUM,SUM(HOURS_PER_ATTRACT) FROM HOURS GROUP BY EMP_NUM;

--QNO4:
SELECT ATTRACT_NO,MIN(HOUR_RATE),MAX(HOUR_RATE) FROM HOURS GROUP BY ATTRACT_NO;

--QNO6:
SELECT * FROM EMPLOYEE CROSS JOIN HOURS;

--QNO7:
SELECT EMPLOYEE.EMP_FNAME,EMPLOYEE.EMP_LNAME,HOURS.ATTRACT_NO,HOURS.DATE_WORKED FROM EMPLOYEE JOIN HOURS ON 
EMPLOYEE.EMP_NUM=HOURS.EMP_NUM;





